This directory holds `.llb` regression fixtures that are intentionally kept
outside `testdata/corpus/` so the resource-coverage dashboard remains scoped to
the current VI/CTL corpus.

`empty-libfile.llb` was copied from the `pylabview` example set and is used for
Phase 5.3 parser/CLI round-trip tests around LabVIEW library containers
(`LVAR`).

