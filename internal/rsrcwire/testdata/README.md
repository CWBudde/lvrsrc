Fixtures in this directory were copied from `../labview_mcp/LabVIEW_Server/`
to keep `internal/rsrcwire` tests self-contained.

- `config-data.ctl`: small control file used for corpus-backed parser coverage
- `get-vi-description.vi`: small VI file used for corpus-backed parser coverage
